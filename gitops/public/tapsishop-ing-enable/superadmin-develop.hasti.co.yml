apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: supper-admin-develop
  namespace: develop
  annotations:
    nginx.ingress.kubernetes.io/proxy-buffer-size: 128k
    nginx.ingress.kubernetes.io/proxy-buffering: 'on'
    nginx.ingress.kubernetes.io/proxy-buffers-number: '8'
spec:
  ingressClassName: nginx
  rules:
  - host: superadmin-develop.hasti.co
    http:
      paths:
      - backend:
          service:
            name: superadmin-webapi
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - superadmin-develop.hasti.co
    secretName: hasti-tls

