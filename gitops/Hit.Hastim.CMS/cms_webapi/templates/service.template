apiVersion: v1
kind: Service
metadata:
  name: $SERVICE_NAME
  namespace: $BRANCHE_NAME
spec:
  ports:
    - port: 80
      targetPort: 80
      name: api
  selector:
    app.kubernetes.io/name: $SERVICE_NAME