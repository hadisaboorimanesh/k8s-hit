apiVersion: v1
kind: Service
metadata:
  name: frontend-vendor
  namespace: develop
spec:
  ports:
    - port: 80
      targetPort: 4001
      name: api
    - port: 3001
      targetPort: 3001
      name: websocket      
  selector:
    app.kubernetes.io/name: frontend-vendor