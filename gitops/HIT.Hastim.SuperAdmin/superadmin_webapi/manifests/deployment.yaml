apiVersion: apps/v1
kind: Deployment
metadata:
  name: superadmin-webapi
  namespace: develop
  labels:
    image-tag: "20250105.5"
    app.kubernetes.io/name: superadmin-webapi
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: superadmin-webapi
  template:
    metadata:
      labels:
        app.kubernetes.io/name: superadmin-webapi
        image-tag: "20250105.5"
    spec:
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: "app.kubernetes.io/name"
                  operator: In
                  values:
                    - superadmin-webapi
              topologyKey: "kubernetes.io/hostname"
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: environment
                operator: In
                values:
                - test
      restartPolicy: Always
      imagePullSecrets:
        - name: hasti-docker-repo
      containers:
        - image: docker.hasti.co/hastim/superadmin_webapi:20250105.5
          name: superadmin-webapi
          command: ["dotnet", "HIT.Hastim.SuperAdmin.Web.dll"]
          envFrom:
            - configMapRef:
                name: superadmin-webapi
          ports:
            - containerPort: 80
              name: api
              protocol: TCP
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "0.5Gi"
              cpu: "0.1"
            limits:
              memory: "2Gi"
              cpu: "1"