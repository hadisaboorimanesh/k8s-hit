apiVersion: v1
kind: Service
metadata:
  name: wallet-webapi
  namespace: develop
spec:
  ports:
    - port: 80
      targetPort: 80
      name: api
  selector:
    app.kubernetes.io/name: wallet-webapi