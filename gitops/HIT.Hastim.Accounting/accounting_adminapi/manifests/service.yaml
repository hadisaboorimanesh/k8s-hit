apiVersion: v1
kind: Service
metadata:
  name: accounting-adminapi
  namespace: develop
spec:
  ports:
    - port: 80
      targetPort: 80
      name: api
  selector:
    app.kubernetes.io/name: accounting-adminapi