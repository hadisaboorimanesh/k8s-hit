version: '3.3'

services:
  chatbot-rasa:
    image: $CI_DOCKER_REGISTRY/$CI_PROJECT_PATH:${CI_COMMIT_REF_NAME}
    restart: always
    ports:
      - "8080:80"

networks:
  default:
    external:
      name: develop
