---
apiVersion: v1
kind: Service
metadata:
  name: chatbot-dashboard
  namespace: main
spec:
  selector:
    app.kubernetes.io/name: chatbot-dashboard
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
